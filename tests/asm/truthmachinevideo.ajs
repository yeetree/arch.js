!,This is the label truth machine, but with video!;
!,I made this one output to the video port instead, so you can see it better!

!,Input character and display it;
inb,a,0;
outb,a,1;

!,See if it is equal to '1';
!,This sets the flags register to 2 if it is equal;
!,If not, it will be set to 0;
cmp,a,'1';

!,If it is, jump past next code block;
lda,:true;
jnz,f;

!,Else, do nothing;
label,false;
lda,:false;
jnz,1;

!,forever print '1';
label,true;
lda,:true;
mw,a,'1';
outb,a,1;
jnz,1;